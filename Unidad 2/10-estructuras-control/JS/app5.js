const autenticado = true;
const puedePagar = true;

// Validar si una condicion con el operador ternario '?'
console.log(autenticado ? 'Usuario autenticado' : 'Usuario no auntenticado');

// Utilizando el operador && 'Y' y verificando con el operador ternario
console.log(autenticado && puedePagar ? 'Usuario autenticado puede pagar' : 'Usuario no auntenticado no puede pagar');


