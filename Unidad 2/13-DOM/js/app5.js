//Este nos devuelve un arreglo donde podemos recorrerlo

const cards = document.querySelectorAll('.card');
console.log(cards);

const noExiste = document.querySelectorAll('.no-existe');
console.log(noExiste);